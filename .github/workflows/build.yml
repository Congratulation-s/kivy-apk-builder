name: Build Kivy APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies
        run: |
          sudo apt-get update -qq
          # 🔥 ส่วนสำคัญ: เพิ่มไลบรารีกราฟิก SDL2 ทั้งระบบเพื่อแก้ปัญหาความขัดแย้งของ Cython และปิดบั๊ก config.pxi
          sudo apt-get install -qq -y scons build-essential python3-dev libffi-dev ssl-cert autoconf automake libtool pkg-config libltdl-dev zlib1g-dev libncurses5-dev libsqlite3-dev libstdc++6 zip unzip libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev

      - name: Install Buildozer and Compatible Cython
        run: |
          pip install --quiet --upgrade pip
          # 🔥 ขยับรุ่น Cython มาเป็น 0.29.36 เพื่อให้เสถียรกับการคอมไพล์สถาปัตยกรรมกราฟิกยุคใหม่
          pip install --quiet buildozer cython==0.29.36

      - name: Prepare Files
        run: |
          find . -name "buildozer.spec" -exec cp {} . \;
          find . -name "main.py" -exec cp {} . \;

      - name: Compile Kivy APK
        id: compile
        run: |
          export ANDROID_AUTO_ACCEPT_SDK_LICENSES="true"
          buildozer -v android debug > full_build.log 2>&1

      - name: Show Error Log Only If Failed
        if: failure()
        run: |
          echo "=========================================================="
          echo "🔍 SCANNING FOR ACTUAL ERRORS IN LOG FILE..."
          echo "=========================================================="
          grep -i -C 20 -E "error|exception|failed" full_build.log || tail -n 100 full_build.log
          echo "=========================================================="

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: m3u8-player-apk
          path: bin/*.apk
