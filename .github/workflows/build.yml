name: Build Kivy APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Prepare Workspace Files
        run: |
          # บังคับย้ายไฟล์คอนฟิกหลักมาอยู่ที่โฟลเดอร์ด้านนอกสุดเพื่อป้องการปัญหาระบบหาไฟล์ไม่เจอ
          find . -name "buildozer.spec" -exec cp {} . \;
          find . -name "main.py" -exec cp {} . \;

      - name: Build APK with Buildozer Action
        # 🔥 แก้ไขจาก ArtemGovorov เป็น ArtemSBulgakov ซึ่งเป็นเวอร์ชันสากลและอัปเดตล่าสุดในปัจจุบัน
        uses: ArtemSBulgakov/buildozer-action@v1
        with:
          workdir: .
          buildozer_version: stable
        env:
          # แมปตำแหน่งของเครื่องมือจำลอง Android SDK/NDK แท้ของระบบเพื่อความถูกต้อง
          ANDROID_SDK_PATH: /usr/local/lib/android/sdk
          ANDROID_NDK_PATH: /usr/local/lib/android/sdk/ndk/27.3.13750724

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: m3u8-player-apk
          path: bin/*.apk
